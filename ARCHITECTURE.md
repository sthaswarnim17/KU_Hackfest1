# Aero-Gauge Architecture Visualization

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    AERO-GAUGE SYSTEM                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    1. DATA INPUT                             │
└─────────────────────────────────────────────────────────────┘

    📷 Image Upload/Camera
           │
           ↓
    ┌──────────────┐
    │ PIL Image    │
    │ (RGB, any    │
    │  resolution) │
    └──────────────┘


┌─────────────────────────────────────────────────────────────┐
│                 2. PREPROCESSING                             │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────┐
    │           Image Preprocessing                     │
    │  • Resize to 256×256                             │
    │  • Center crop to 224×224                        │
    │  • Convert to tensor                             │
    │  • Normalize (ImageNet stats)                    │
    └──────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│              3. DUAL-PATH FEATURE EXTRACTION                 │
└─────────────────────────────────────────────────────────────┘

    ┌────────────────────┐          ┌────────────────────┐
    │  VISION PATH       │          │  PHYSICS PATH      │
    │  (Deep Learning)   │          │  (Handcrafted)     │
    └────────────────────┘          └────────────────────┘
           │                                 │
           ↓                                 ↓
    ┌──────────────┐              ┌────────────────────┐
    │  ResNet18    │              │ Dark Channel Prior │
    │  Backbone    │              │   • dark_channel() │
    │              │              │   • transmission() │
    │ Pretrained   │              │   • atm_light()    │
    │ on ImageNet  │              ├────────────────────┤
    │              │              │ Image Quality      │
    │ Layer 1-3:   │              │   • laplacian_var  │
    │   Frozen     │              │   • contrast       │
    │ Layer 4:     │              │   • rms_contrast   │
    │   Trainable  │              ├────────────────────┤
    └──────────────┘              │ Color Features     │
           │                      │   • color_atten    │
           ↓                      │   • edge_density   │
    ┌──────────────┐              └────────────────────┘
    │   512-dim    │                       │
    │  features    │                       ↓
    └──────────────┘              ┌────────────────────┐
                                  │   24-dim features  │
                                  │   (normalized)     │
                                  └────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                   4. FEATURE FUSION                          │
└─────────────────────────────────────────────────────────────┘

           512-dim                24-dim
              │                     │
              └──────────┬──────────┘
                         │
                         ↓
                   ┌─────────────┐
                   │  Concat     │
                   │  (536-dim)  │
                   └─────────────┘
                         │
                         ↓
                   ┌─────────────┐
                   │  FC Layer   │
                   │  536 → 256  │
                   │  + ReLU     │
                   │  + Dropout  │
                   └─────────────┘
                         │
                         ↓
                   ┌─────────────┐
                   │  FC Layer   │
                   │  256 → 128  │
                   │  + ReLU     │
                   │  + Dropout  │
                   └─────────────┘
                         │
                         ↓
                   ┌─────────────┐
                   │  FC Layer   │
                   │  128 → 1    │
                   └─────────────┘


┌─────────────────────────────────────────────────────────────┐
│                   5. OUTPUT & POST-PROCESSING                │
└─────────────────────────────────────────────────────────────┘

                   ┌─────────────┐
                   │ log(PM2.5)  │
                   │ prediction  │
                   └─────────────┘
                         │
                         ↓ exp(x) - 1
                   ┌─────────────┐
                   │  PM2.5      │
                   │  (µg/m³)    │
                   └─────────────┘
                         │
                         ├────────────────────────┐
                         ↓                        ↓
                  ┌─────────────┐        ┌──────────────┐
                  │ AQI Category│        │ Health       │
                  │ • Good      │        │ Advisory     │
                  │ • Moderate  │        │              │
                  │ • Unhealthy │        │              │
                  │ • etc.      │        │              │
                  └─────────────┘        └──────────────┘


┌─────────────────────────────────────────────────────────────┐
│                   6. EXPLAINABILITY                          │
└─────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │  Dark Channel Heatmap                          │
    │  • Visual representation of haze intensity     │
    │  • Color-coded (blue=clear, red=hazy)         │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │  Top Contributing Features                     │
    │  • Feature name                                │
    │  • Raw value                                   │
    │  • Normalized z-score                          │
    └────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────┐
    │  Confidence Estimate                           │
    │  • Model uncertainty (if implemented)          │
    │  • Reliability indicator                       │
    └────────────────────────────────────────────────┘
```

---

## Training Pipeline Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   TRAINING WORKFLOW                          │
└─────────────────────────────────────────────────────────────┘

1. DATA LOADING
   └─→ Load metadata.csv
       └─→ Split: Train (80%) / Val (10%) / Test (10%)
           └─→ Stratify by PM2.5 ranges

2. FEATURE EXTRACTION (Once, then cached)
   └─→ For each image:
       └─→ Extract 24 physics features
           └─→ Save to cache/

3. NORMALIZATION
   └─→ Compute mean/std from training set
       └─→ Save to outputs/feature_normalization.json

4. TRAINING LOOP (30 epochs)
   └─→ For each epoch:
       ├─→ Training phase
       │   ├─→ Forward pass (batch_size=16)
       │   ├─→ Compute MSE loss (log-scale)
       │   ├─→ Backward pass
       │   └─→ Update weights (AdamW)
       │
       ├─→ Validation phase
       │   ├─→ Forward pass (no grad)
       │   ├─→ Compute metrics (MAE, RMSE, Spearman)
       │   └─→ Check for improvement
       │
       └─→ Checkpointing
           ├─→ If val MAE improved: save model
           └─→ If patience exceeded: early stop

5. EVALUATION
   └─→ Load best model
       └─→ Test on hold-out set
           └─→ Generate plots
               └─→ Save results


TIME ESTIMATE:
• Feature extraction: 5-10 min (once)
• Training (GPU): 15-30 min
• Training (CPU): 2-4 hours
```

---

## Streamlit App Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   STREAMLIT APP FLOW                         │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│  App Startup │
└──────────────┘
       │
       ↓
┌──────────────────────┐
│ Load Model (cached)  │
│ • best_model.pt      │
│ • normalization.json │
└──────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────┐
│  Main Interface                               │
│  ┌──────────────────────────────────────┐   │
│  │  Image Input                         │   │
│  │  • File upload OR                    │   │
│  │  • Camera capture                    │   │
│  └──────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
       │
       ↓ [User clicks "Analyze"]
       │
┌──────────────────────────────────────────────┐
│  Processing                                   │
│  1. Preprocess image                         │
│  2. Extract physics features                 │
│  3. Run model inference                      │
│  4. Generate explanations                    │
└──────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────┐
│  Results Display                              │
│  ┌────────────────────────────────────┐     │
│  │  PM2.5 | AQI | Category           │     │
│  └────────────────────────────────────┘     │
│  ┌────────────────────────────────────┐     │
│  │  Health Advisory                   │     │
│  └────────────────────────────────────┘     │
│  ┌────────────────────────────────────┐     │
│  │  Explainability Tabs               │     │
│  │  • Dark Channel Heatmap            │     │
│  │  • Top Features                    │     │
│  │  • All Features                    │     │
│  └────────────────────────────────────┘     │
│  ┌────────────────────────────────────┐     │
│  │  Export Options                    │     │
│  │  • Download JSON                   │     │
│  │  • Download TXT                    │     │
│  └────────────────────────────────────┘     │
└──────────────────────────────────────────────┘

PERFORMANCE:
• Model loading: 2-3 seconds (once, cached)
• Inference: 200-300ms per image (CPU)
• Feature extraction: 100-200ms
```

---

## Physics Feature Extraction Detail

```
Dark Channel Prior Algorithm:
────────────────────────────────

Input: RGB Image I(x) ∈ [0, 255]³

1. For each pixel x:
   dark(x) = min(I^c(y))
             c∈{r,g,b}
             y∈Ω(x)
   
   where Ω(x) = 15×15 window around x

2. Atmospheric Light A:
   • Find top 0.1% brightest pixels in dark(x)
   • Select pixel with max intensity in I(x)
   • A ∈ ℝ³

3. Transmission Map:
   t(x) = 1 - ω · dark(I(x)/A)
   where ω = 0.95 (haze retention)

Output: 6 statistics from dark(x), 4 from t(x), 4 from A

────────────────────────────────

Other Features:

• Laplacian Variance:
  var(∇²I) → measures sharpness

• Contrast:
  σ(I) / μ(I) → std/mean ratio

• Color Attenuation:
  1 - saturation(bright pixels)

Total: 24 features → 24-dim vector
```

---

## Module Dependencies

```
┌─────────────────────────────────────────────────┐
│               MODULE RELATIONSHIPS               │
└─────────────────────────────────────────────────┘

features.py
   └─→ uses: cv2, numpy

utils.py
   └─→ uses: pandas, sklearn, scipy

dataset.py
   ├─→ imports: features.py
   └─→ uses: torch, PIL

model.py
   └─→ uses: torch, torchvision

train.py
   ├─→ imports: dataset.py, model.py, utils.py
   └─→ uses: torch, matplotlib

inference.py
   ├─→ imports: model.py, features.py, utils.py
   └─→ uses: torch, PIL

app.py
   ├─→ imports: inference.py, features.py
   └─→ uses: streamlit, matplotlib

DEPENDENCY ORDER:
1. features.py, utils.py (no internal deps)
2. dataset.py, model.py
3. train.py, inference.py
4. app.py
```

---

## File Size Estimates

```
Source Code:         ~100 KB
Documentation:       ~150 KB
Requirements:        ~1 KB
Tests:              ~30 KB

Dataset (typical):   1-10 GB
Model weights:       ~45 MB
Cached features:     ~5-50 MB
Training outputs:    ~10 MB

Total (no dataset):  ~250 MB
Total (with data):   1-10 GB
```

---

## Performance Characteristics

```
┌─────────────────────────────────────────────────┐
│            PERFORMANCE METRICS                   │
└─────────────────────────────────────────────────┘

Feature Extraction:
  • Single image:    100-200 ms
  • Batch of 1000:   2-3 minutes
  • Cached:          0 ms (instant)

Training:
  • CPU (30 epochs): 2-4 hours
  • GPU T4:          15-30 minutes
  • GPU A100:        5-10 minutes

Inference:
  • CPU single:      200-300 ms
  • GPU single:      50-100 ms
  • Batch 32 (GPU):  1-2 seconds

Memory Usage:
  • Model:           ~200 MB
  • Training:        2-4 GB (RAM)
  • Training (GPU):  2-3 GB (VRAM)
  • Inference:       500 MB

Disk Space:
  • Source + docs:   ~250 MB
  • Model weights:   ~45 MB per model
  • Feature cache:   ~50 MB per 1000 images
```

---

This architecture enables:
✅ Fast inference (200-300ms)
✅ Explainable predictions
✅ Robust to small datasets (transfer learning)
✅ Offline capable (no API dependencies)
✅ Easy to deploy (single model file)
